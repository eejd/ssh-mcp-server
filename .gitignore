# 依赖目录
node_modules/
# package-lock.json is tracked in this fork — the Dockerfile's `npm ci` needs
# it committed for reproducible ghcr builds (agent-services-hive#223).

# 构建输出
build/

# 操作系统文件
.DS_Store
Thumbs.db

# 编辑器和IDE文件
.idea/
.vscode/
*.swp
*.swo
.project
.classpath
.settings/
memo.md

# claude
.claude/
.mcp.json
docs

# codex
.codex/
